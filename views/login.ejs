<!-- views/login.ejs -->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #f4f4f4;
      }

      form {
        max-width: 400px;
        width: 100%;
        padding: 20px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        background-color: #fff;
        border-radius: 8px;
      }

      h2 {
        text-align: center;
        color: #333;
      }

      label {
        display: block;
        margin-bottom: 8px;
        color: #555;
      }

      .phone-number-group {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 16px;
      }

      #internationalCode {
        width: 30%;
        padding: 8px;
        margin-right: 10px; /* Added margin between international code and phone number */
        border: 1px solid #ddd;
        border-radius: 4px;
      }

      #phoneNumber {
        width: 70%;
        padding: 8px;
        border: 1px solid #ddd;
        border-radius: 4px;
      }

      #phoneNumber::placeholder {
        color: #999;
      }

      button {
        width: 100%;
        padding: 10px;
        background-color: #4caf50;
        color: #fff;
        border: none;
        border-radius: 4px;
        cursor: pointer;
      }

      button:hover {
        background-color: #45a049;
      }
    </style>
  </head>

  <body>
    <form action="/auth/login" method="post">
      <h2>Login with Phone Number</h2>
      <label for="phoneNumberGroup">Phone Number:</label>
      <div class="phone-number-group">
        <select id="internationalCode" name="internationalCode" required>
          <option value="+254">+254 (Kenya)</option>
          <!-- Add more options as needed -->
        </select>
        <input
          type="tel"
          id="phoneNumber"
          name="phoneNumber"
          pattern="^07[0-9]{8}$"
          title="Enter a valid Kenyan phone number"
          required
          placeholder="07xxxxxxxx"
        />
      </div>
      <button type="submit">Send Code</button>
    </form>
  </body>
</html>
